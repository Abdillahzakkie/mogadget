node_modules/
.next/
dist/
*.tsbuildinfo
# Ignore every env file (they hold secrets) except the committed template.
.env
.env.*
!.env.example
.DS_Store
.uploads/

coverage/
test-results/
playwright-report/
playwright/.cache/

# Next.js generates this on every dev/build (churns between dev/prod paths)
next-env.d.ts
